{
  "buildCommand": "pnpm install && pnpm --filter @workspace/birthday-cake run build",
  "outputDirectory": "artifacts/birthday-cake/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}

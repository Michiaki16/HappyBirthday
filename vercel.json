{
  "buildCommand": "pnpm install && pnpm --dir birthday-cake-app run build",
  "outputDirectory": "birthday-cake-app/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
